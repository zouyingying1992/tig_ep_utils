name: tig_ep_utils
description: Basic Flutter/Dart classes for ESC/POS printing. Ticket class generates ESC/POS commands that can be sent to a thermal printer.andrey-ushakov/esc_pos_utils
version: 0.0.4
homepage: https://github.com/liunina/tig_ep_utils

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hex: ^0.2.0
  gbk_codec: ^0.4.0
  image: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
